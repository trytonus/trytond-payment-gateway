<?xml version="1.0"?>
<!-- The COPYRIGHT file at the top level of
this repository contains the full copyright notices and license terms. -->
<tree string="Transactions"
        colors="If(In(Eval('state', ''), ['in-progress', 'authorized', 'completed']), 'blue', If(In(Eval('state', ''), ['failed', 'cancel']), 'red', If(Equal(Eval('state', ''), 'posted'), 'green', 'black')))">
    <field name="date"/>
    <field name="uuid"/>
    <field name="party"/>
    <field name="amount"/>
    <field name="currency"/>
    <field name="gateway"/>
    <field name="state"/>
</tree>
